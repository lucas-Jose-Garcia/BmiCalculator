import React, { useState } from "react";
import { View, Text, Modal, Button } from "react-native";

interface TableModalProps {
  isModalVisible: boolean
  toggleModal: () => void
}

export function TableModal({isModalVisible, toggleModal} : TableModalProps) {
  return (
    <></>
  );
}